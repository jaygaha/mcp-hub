# MCP Hub frontend

Next.js (App Router) UI for [MCP Hub](../README.MD) — see the root README for
setup, the `Makefile` targets, and how this talks to the backend.

```bash
cp .env.example .env.local
npm install
npm run dev
```
